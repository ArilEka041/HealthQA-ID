# HealthQA-ID — dependencies
# Pasang semua dengan: pip install -r requirements.txt

# Core
pandas>=2.0
python-dotenv>=1.0.0
requests>=2.28

# Google AI (Gemini / Gemma via Google API)
# Pilih salah satu atau keduanya:
google-genai>=0.8.0          # SDK baru (direkomendasikan)
google-generativeai>=0.8.0   # SDK lama — fallback

# OpenAI (untuk ChatGPT)
openai>=1.0.0

# Model lokal (Ollama) tidak perlu dependensi Python tambahan
# — komunikasi dilakukan via HTTP ke http://localhost:11434